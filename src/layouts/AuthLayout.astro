---
import "../styles/global.css";
import { ViewTransitions } from "astro:transitions";
import { Toaster } from "@/components/ui/sonner";

interface Props {
  title?: string;
  description?: string;
}

const {
  title = "IntelliXCards â€” Strefa konta",
  description = "Zaloguj siÄ™ lub utwĂłrz konto, aby korzystaÄ‡ z IntelliXCards.",
} = Astro.props satisfies Props;

const htmlClasses = ["dark", "h-full"];
---

<!doctype html>
<html lang="pl" class:list={htmlClasses}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/png" href="/favicon.png" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body class="min-h-screen font-sans antialiased">
    <ViewTransitions>
      <div class="relative flex min-h-screen flex-col">
        <div class="pointer-events-none absolute inset-0 overflow-hidden">
          <div class="absolute inset-0 bg-gradient-to-br from-indigo-950 via-purple-950 to-blue-900" />
          <div class="absolute inset-0 bg-[radial-gradient(circle_at_top,_rgba(59,130,246,0.2),_transparent_65%)]" />
          <div class="absolute inset-0 bg-[radial-gradient(circle_at_bottom,_rgba(236,72,153,0.15),_transparent_60%)]" />
          <div class="absolute inset-0 bg-[linear-gradient(130deg,_rgba(255,255,255,0.08)_0%,_rgba(255,255,255,0.02)_35%,_transparent_70%)]" />
        </div>

        <div class="relative z-10 flex min-h-screen flex-col">
          <header class="px-6 pb-4 pt-8 text-white/90">
            <div class="mx-auto flex w-full max-w-5xl flex-col gap-6 sm:flex-row sm:items-center sm:justify-between">
              <a href="/" class="inline-flex items-center gap-2 font-semibold tracking-tight text-white">
                <span class="rounded-full bg-white/15 px-3 py-1 text-sm uppercase">10x</span>
                <span class="text-lg sm:text-xl">IntelliXCards</span>
              </a>
              <nav class="flex flex-wrap items-center gap-3 text-sm text-white/70">
                <a class="transition hover:text-white" href="/">PowrĂłt na stronÄ™ gĹ‚ĂłwnÄ…</a>
                <span aria-hidden="true" class="hidden sm:inline-block text-white/30">â€˘</span>
                <a class="transition hover:text-white" href="#support">
                  Potrzebujesz pomocy?
                </a>
              </nav>
            </div>
          </header>

          <main class="flex flex-1 items-center justify-center px-4 pb-16 pt-6 sm:px-6">
            <div class="relative z-10 flex w-full justify-center">
              <slot />
            </div>
          </main>

          <footer class="px-6 pb-8 text-xs text-white/60">
            <div class="mx-auto flex w-full max-w-5xl flex-col items-center justify-between gap-3 sm:flex-row">
              <p class="text-center sm:text-left">Â© {new Date().getFullYear()} IntelliXCards. Wszelkie prawa zastrzeĹĽone.</p>
              <div class="flex flex-wrap items-center gap-3">
                <a class="transition hover:text-white" href="#privacy">
                  Polityka prywatnoĹ›ci
                </a>
                <span aria-hidden="true" class="text-white/30">â€˘</span>
                <a class="transition hover:text-white" href="#terms">
                  Regulamin
                </a>
                <span aria-hidden="true" class="text-white/30">â€˘</span>
                <a class="transition hover:text-white" href="#support">
                  Wsparcie
                </a>
              </div>
            </div>
          </footer>
        </div>
      </div>
    </ViewTransitions>
    <Toaster client:load />
  </body>
</html>
